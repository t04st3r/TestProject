<Alloy>
	<Collection src="item"/> 
    <Window class="container">
        <TableView dataCollection="item">
            <TableViewRow name="{name}" description="{description}" address="{address}"></TableViewRow>
        </TableView>
    </Window>
</Alloy>